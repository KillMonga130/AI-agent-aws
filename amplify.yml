version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No preBuild step for static site"
    build:
      commands:
        - echo "No build step - serving /web as artifact"
  artifacts:
    baseDirectory: web
    files:
      - '**/*'
  cache:
    paths: []
